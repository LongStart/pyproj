export PYTHONPATH=$PYTHONPATH:$(builtin cd "`dirname "${BASH_SOURCE[0]}"`" > /dev/null && pwd)
export PYTHONPATH=$PYTHONPATH:$(builtin cd "`dirname "${BASH_SOURCE[0]}"`" > /dev/null && pwd)/../plot_collection
export PYTHONPATH=$PYTHONPATH:$(builtin cd "`dirname "${BASH_SOURCE[0]}"`" > /dev/null && pwd)/../bspline_manual